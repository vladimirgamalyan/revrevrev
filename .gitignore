# Credentials, compiled into the firmware (ADR-0004). Never commit.
secrets.h

# ESP-IDF build output
build/

# Generated by menuconfig; sdkconfig itself is committed
sdkconfig.old

# Fetched by the IDF component manager
managed_components/
dependencies.lock
